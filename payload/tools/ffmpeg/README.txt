Bundled FFmpeg location for screen recording

Place ffmpeg binary here:
- tools/ffmpeg/bin/ffmpeg.exe

Notes:
- The script now auto-detects bundled FFmpeg first.
- If not found, it falls back to PATH ffmpeg (if available).
- You can override path in GUI: screen recording > ffmpeg.exe.
